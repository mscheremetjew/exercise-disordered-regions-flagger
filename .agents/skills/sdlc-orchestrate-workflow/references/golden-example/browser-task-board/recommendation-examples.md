# Recommendation Examples

## What Next?

Finding: `Implementation` is in progress and points to `i-validate-user-story-completion`.

Recommendation: Run `i-validate-user-story-completion` for the implemented story named in the trace.

## Is This Ready For PR?

Finding: `User story validation` is `Not Started`.

Recommendation: Not ready for `h-create-implementation-pull-request`; run `i-validate-user-story-completion` first.

## Can We Release?

Finding: `Release deployment` is `Not Started`, while pull request evidence is missing.

Recommendation: Complete PR review or record the approved release path before `j-prepare-release-deployment`.
